export interface IHeroWords {
    text: string;
}

export interface IItWords {
    text:string,
}

export interface ILanguageCards {
    languageName:string,
    backgroundImage:string,
    activeStatus:number,
    inactiveStatus:number,
    markedStatus:number,
    class:string
}